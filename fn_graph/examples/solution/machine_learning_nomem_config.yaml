pipeline:
  run_id: ml_run_nomem_001
  on_failure: finish_running

artifact_store:
  type: fs
  base_dir: ./artifacts

nodes:
  # all-memory config for testing without Docker daemon
  "*":
    executor: memory
