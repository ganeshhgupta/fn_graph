pipeline:
  run_id: finance_run_001
  on_failure: finish_running

artifact_store:
  type: fs
  base_dir: ./artifacts

nodes:
  # all nodes run in-process — no Docker/Lambda needed for the finance pipeline
  "*":
    executor: memory
